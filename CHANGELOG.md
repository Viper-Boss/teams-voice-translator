# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.14.7] - 2026-09-07

### 答辩模式

- 麦克风、程序输出、对方回环和试听设备都使用真实禁用状态的锁定控件，避免锁定后仍能展开或误改。
- 保存对方回环后，主界面立即显示选择/切换状态；实际监听建立后更新为当前监听设备。
- 新增七种自然英语语气预设和自定义指令；按模型系列发送正确的 `instruction` 或 `instructions` 参数，不支持的模型不会误发。
- 新增四档本地合成房间底噪，在英文语音下维持轻微噪声并在句尾淡出，默认关闭且不产生云端费用。

### 验证

- 324 项离线测试通过，覆盖设备锁定、设备名称联动、语气模型兼容和环境底噪处理。

## [1.14.6] - 2026-09-06

### 修复

- 评委监听设备同时保存名称和编号；Windows 重启或声卡驱动改变设备编号后，会按名称重新匹配真实扬声器或耳机。
- 评委回环禁止选择 CABLE 虚拟输出，防止程序自己的原声和英文播音被识别成评委发言；F5 原声期间暂停评委识别，并屏蔽结束后的短暂尾音。
- 完整英文译文拆分后，CosyVoice 和 HTTP TTS 最多并发预生成三段音频，再由单一播放器按原句顺序播放，减少句间等待且不打乱内容。

### 验证

- 316 项离线测试通过，新增设备编号迁移、CABLE 防回灌、原声屏蔽及并行合成顺序测试。

## [1.14.5] - 2026-09-06

### 答辩模式

- 新增完整答辩工作流：评委英文回环识别与中文字幕、中文整句上下文翻译、克隆音色英文发声、PPT 摘要和术语导入、AI 参考回答、讲稿预习及双语记录。
- 支持 CosyVoice v3.5 与 Qwen3 TTS VC 音色复刻，集成参考音频最长时长、预处理和音量归一化选项；自然模式会在完整译文和音频就绪后播放。
- 默认使用按住说话：F5 原声、F9 翻译，松开立即停止收音。持续监听只改变按钮操作方式，由用户选择原声或翻译，不会自动启动或回到上一模式。

### 稳定性

- 将麦克风声卡的打开、阻塞读取和关闭统一到专属线程，修复延迟启动、音频回调和资源释放方面的竞争条件。
- 自检界面所有更新回到 Qt 主线程；停止、关闭和任务代次会丢弃迟到回调，降低播放完成或关闭窗口时的崩溃风险。
- 修复 TTS 队列、连接重建、取消、HTTP 响应释放、PCM 奇数字节分包和播放器失效流处理。

### 界面与练习

- 讲稿预习支持长句自动换行、按播放进度移动的黄色高亮字幕，以及当前句的暂停、继续和停止。
- 声音对比首次试听和本机缓存重听均支持停止和底部字幕。
- 设置页锁定按钮显示当前状态；所有下拉框和数字输入框忽略鼠标滚轮，防止滚动页面时误改参数。

### 验证

- 310 项离线测试通过；打包版完成隔离启动和 50 轮自检播放压力测试。测试不会访问云端，也不会采集真实音频。

## [1.11.4-local] - 2026-09-01

### GitHub 哈希显示修复

- GitHub 最新版本卡片不再显示“含 SHA-256”，改为直接显示发布文件完整的 64 位 SHA-256。
- 哈希统一为小写，并支持用鼠标选中复制；GitHub 未提供哈希时明确显示“官方未提供”。
- 增加最新版本卡片完整哈希显示回归测试；完整测试共 125 项。

## [1.11.3-local] - 2026-09-01

### VoiceStudio 安装流程修复

- 安装、升级、卸载和手动安装包校验期间，操作按钮会切换为独立任务面板，不再与下载文字和进度条重叠。
- 新增“取消任务”按钮；可中止正在进行的下载、SHA-256 校验和 Windows Installer，并清理未完成的下载文件。
- 安装完成后在 0、1.5、4、8 秒分阶段刷新安装版本和进程状态，并继续刷新本地声音库，避免第一次探测尚未完成时状态停留在“未安装”。
- 兼容 VoiceStudio 0.5.1 实际使用的 `omnivoice-studio.exe` 文件名；注册表显示 0.5.1 时会正确识别安装目录、版本和启动程序。
- 新增取消、安装程序终止、当前桌面程序识别、任务面板和延迟刷新测试；完整测试共 124 项。

## [1.11.2-local] - 2026-08-31

### 视觉细节与顶部布局修复

- VoiceStudio 八个生命周期按钮统一改用 Phosphor 细线矢量图标，并重新调整按钮边框、圆角和交互状态。
- 修复版本信息卡片的三个内部标签继承页面底色而形成横向色带的问题；卡片内部现为完整连续背景。
- 修复水晶极光顶部标题被旧的背景选择框遮挡或挤出可视区域的问题；三个快捷选择框从顶部布局移除后会同步隐藏，标题区保留最小完整宽度。
- 新增界面回归测试，覆盖顶部控件残留、标题区域宽度、统一图标族和版本卡片透明标签；完整测试共 119 项。

## [1.11.1-local] - 2026-08-31

### 实机界面配置修复

- 修复旧配置 `ui_theme_revision=2` 未被迁移，导致新版首次启动仍显示“午夜工作台 + Fluent 云白”、与指定水晶极光参考稿完全不一致的问题。
- 本次升级只将旧版视觉配置一次性迁移到“水晶极光 + 冰川水晶蓝”，API Key、音频设备、模型、音色、快捷键和课程配置全部保持不变。
- 迁移完成后仍可手动选择其他布局与配色，且不会在后续启动时重复重置。
- 增加当前修订和旧修订的配置迁移测试，防止本地成品与开发预览再次出现不同布局。

## [1.11.0-local] - 2026-08-31

### 水晶极光三大页面统一精修

- 以水晶极光参考稿为统一审美基准，重构顶部品牌区、主页面签、半透明内容容器、状态胶囊和矢量图标体系。
- 会议控制台新增真实的左侧功能导航，实时翻译、快捷输入/朗读和会议记录均可一键定位；背景立绘与快捷键卡被整合进同一导航轨道。
- 本地声音工作台重做安装连接、2×4 生命周期操作磁贴、三张版本事实卡、进程监控和声音试听区；修复操作控件实际为 `QToolButton`、旧样式却只命中 `QPushButton` 导致磁贴近似无样式的问题。
- 设置页从旧式长表单升级为左侧分区导航 + 右侧滚动配置工作区，API、翻译与语音、字幕与记录、外观与课程可直接跳转。
- 调整水晶布局默认尺寸和卡片密度，在保持紧凑窗口宽度的同时展示更多底部声音库内容。
- 为运行管理、设置导航、会议导航和主页面签统一使用 QtAwesome 矢量图标，并将 QtAwesome 资源加入依赖和 PyInstaller 收集清单。

### 验证

- 115 项单元测试全部通过。
- 重新生成四套典型布局的会议、本地声音、设置共 12 张离屏截图；重点检查水晶极光三页的导航、卡片、按钮和滚动区域。

## [1.10.0-local] - 2026-08-31

### 按四张参考稿重建整个应用界面

- 不再只改 VoiceStudio 卡片：应用标题区、全局导航、会议控制台、本地声音工作台、设置页和底部状态区均按四套参考稿重新组织。
- 水晶极光采用顶部品牌标题与页签、声音工作台内部二级导航、2×4 大型操作磁贴、横向版本卡和下方进程/声音区。
- Fluent 采用单层左侧应用导航、顶部服务健康状态、四张摘要卡、横向命令栏、中央进程监控和右侧声音检视器。
- 午夜信号采用深色单层侧边导航、三栏声音控制台、竖向版本卡、声音库与试听器，并统一会议页和设置页的暗色工作站结构。
- 暖色工作室采用顶部导航、左右均衡双栏、底部状态栏，以及环境、生命周期、声音库、版本、进程和试听的参考图顺序。
- 四种布局仍与四套配色完全解耦，提供 `4 × 4 = 16` 种组合；切换布局不会覆盖配色，切换配色也不会改变页面结构。
- Fluent 与午夜侧边栏模式下，设置页自动改为单列，水晶与暖色模式恢复双列；彻底移除窄内容区横向滚动和字段截断。
- VoiceStudio 试听区新增语速与音量滑块并接入真实试听参数；版本卡、声音数量、进程状态继续显示真实运行数据。

### 验证

- 110 项单元测试全部通过，覆盖 16 种组合、四套全窗口骨架、VoiceStudio 重排和设置页响应式切换。
- 四套典型搭配的会议页、本地声音页、设置页共 12 张 Qt 离屏预览已完成检查。

## [1.9.0-local] - 2026-08-31

### 按四张参考图重建 VoiceStudio 界面

- VoiceStudio 页面不再只换配色：水晶极光改为左侧导航+宽屏监控，Fluent 改为左导航+中央进程+右侧声音检视器，午夜工作台改为三栏专业控制台，暖色工作室改为均衡双栏。
- 安装路径、版本信息、生命周期、进程监控、声音库和本机试听六个功能卡片会随布局真实重排，功能和数据保持不变。
- 四套布局和四套配色依然完全解耦，严格提供 `4 × 4 = 16` 种组合。
- 版本区会按宽窄自动切换横向/纵向排列；工作区可纵向滚动，四套布局均不再产生横向文字裁切。
- 生命周期操作改为 2×4 快捷按钮阵列，安装、导入、GitHub 下载、启动、停止、重启、升级和卸载更接近参考界面。
- 修复窗口销毁时后台 VoiceStudio 状态查询线程可能输出 `C++ object already deleted` 异常的竞态。

### 验证

- 自动枚举 16 种布局/配色组合，并校验四套 VoiceStudio 卡片坐标确实不同。
- 108 项单元测试全部通过；完成水晶蓝、Fluent 云白、午夜蓝黑和琥珀暖白四组 Qt 离屏渲染检查。

## [1.8.1-local] - 2026-08-30

### 高 DPI 文字裁切修复

- “本地声音工作台 → 运行管理”改为自适应滚动内容区，窗口高度不足时滚动查看，不再压扁路径输入框、生命周期按钮和进程表。
- 路径输入框、选择按钮、刷新按钮以及两排生命周期按钮统一设置 38 px 可靠最小高度。
- 使用布局最小尺寸约束，让 Windows 125%/150% 等系统缩放下优先保留控件完整高度。
- 完成 150% DPI 暖色工作室离屏渲染检查；14 项主界面测试通过。

## [1.8.0-local] - 2026-08-30

### 4 × 4 自由外观系统

- 新增水晶极光、午夜工作台、暖色工作室和 Fluent 控制台四套真实布局；会议区会按布局重新编排控制、字幕、快捷输入、工具带和五行时间轴。
- 冰川水晶蓝、午夜蓝黑、琥珀暖白和 Fluent 云白四套配色从布局中解耦，可任意组合，共 16 种外观。
- 顶部与设置页都可切换布局和配色，选择会立即保存；旧版配置默认平滑迁移到“水晶极光 + 冰川水晶蓝”。
- 各布局拥有独立的窗口尺寸、留白密度、圆角、按钮高度和信息优先级；非水晶布局自动收起本机插画，释放工作区空间。
- 顶部长状态信息改为自动省略并在悬停时显示全文，避免新增选择器后挤压标题或截断按钮。

### 本地 / 云端引擎快捷开关

- 右上角新增“云端引擎 / 本地引擎”两态按钮，与设置页的百炼云语音 / VoiceStudio 本地语音双向同步并立即保存。
- 切换本地引擎时主动刷新 VoiceStudio 运行状态，但不会因服务暂未启动而擅自切回云端。

### 验证

- 完成四套布局对应配色的 Qt 离屏渲染检查。
- 107 项单元测试全部通过，新增 4×4 独立组合、动态重排和本地/云端引擎切换测试。

## [1.7.0-local] - 2026-08-30

### 界面修复

- VoiceStudio 安装路径、桌面程序和本地服务三行的右侧按钮统一加宽，并补充悬停说明，修复 Windows 高 DPI/系统缩放下按钮文字被截断。

### VoiceStudio 生命周期管理

- “本地声音工作台”拆分为“运行管理”和“声音库与试听”，新增安装路径、程序路径、本地服务地址与集中管理按钮。
- 支持从 GitHub 最新 Release 自动下载 Windows x64 MSI，下载阶段实时显示百分比和已下载/总大小，安装阶段显示不定进度。
- 支持手动导入 MSI；自动读取 GitHub 最新版本与官方 SHA-256，匹配时绿色确认，不匹配时红色警告，但允许用户明确选择后继续安装。
- 新增“手动下载（GitHub）”直达官方 Releases、自动安装、卸载、启动、停止、重启和升级。
- 显示已安装版本、本地 API 版本、GitHub 最新版本，以及进程角色、PID、内存、运行时间和命令行；服务正常显示绿色，进程存在但 API 异常显示红色。
- 卸载仅移除程序，默认保留声音、模型、项目和生成记录，避免误删大体积用户数据。
- 当前软件退出时会弹出不可取消的关闭进度框，逐个显示正在关闭的 VoiceStudio 桌面程序和后台子进程，全部处理完再退出主程序。
- 进程识别只接受 VoiceStudio 桌面进程、它的后代以及带 OmniVoice 后端标记的受控子进程，避免误停无关 Python/Node 进程。

### 验证

- GitHub 官方 API 实测可读取 `v0.5.1`、Windows MSI 文件名、文件大小与发布页 SHA-256。
- 新增 Release 指纹解析、文件 SHA-256 进度、生命周期控件、安装目录保存和退出等待回归测试。
- 105 项单元测试全部通过。

## [1.6.0-local] - 2026-08-30

### VoiceStudio 本地声音工作台

- 接入 `debpalash/VoiceStudio` 的本地 OpenAI 兼容语音接口，支持健康检测、声音档案与 TTS 引擎同步。
- 新增独立“本地声音工作台”页：可配置本地地址和桌面程序路径、启动 VoiceStudio、同步声音库、选择模型/音色并本机试听。
- 新增百炼云 / VoiceStudio 本地 TTS 总开关；课程配置会保存本地/云端后端以及对应模型和音色。
- 传统 F9、键盘原文/翻译发声、当前英文播放、历史逐句重播和双语整段朗读统一改走所选 TTS 后端。
- 键盘“按输入原文直接朗读”在使用 VoiceStudio 时不再强制要求百炼 API Key；翻译和 ASR 仍按原设计使用百炼。
- VoiceStudio 返回 PCM 时按 24 kHz 直接流式播放；返回 WAV/压缩音频时自动解码为 PCM，兼容不同本地引擎。
- 保持极速 F9 的百炼一体化实时通路不变，避免拆开后增加延迟。

### 验证

- 101 项单元测试全部通过，新增 VoiceStudio URL、声音库解析、OpenAI 兼容 TTS 请求和主界面路由测试。
- 完成 1180×820 Qt 离屏界面检查；本版本继续只保存在默认工作目录，不发布 GitHub。

## [1.5.0-local] - 2026-08-26

### 长文朗读性能与可靠性

- 当前句开始播放后会后台预合成下一句，显著减少逐句之间等待云端 TTS 的空档。
- 本次朗读会话按译文、音色和 TTS 参数缓存 PCM；重播或跳回已合成句子不会再次请求和计费。
- 修复停止后立即跳句可能与旧播放线程竞争的问题；每个播放线程使用独立停止信号。
- 完成时的已用时间、总时间和时间轴改用各句真实 PCM 时长，不再错误显示旧的预计总时长。
- 后台预取短暂失败时，前台播放会自动做一次干净重试。

### 全文翻译与校对

- 长文语境翻译模型从课堂总结模型中独立，可在高级设置中单独选择。
- 超过 6000 字或 24 句的长稿会先提取全文人物、术语和上下文摘要，再按 18 句一批对齐翻译，避免重复全文导致上下文溢出。
- 全文准备窗口加入取消按钮和请求代次保护；取消后的迟到响应不会再打开旧窗口。
- 双语朗读台加入校对模式，可直接修改英文，并可让模型结合全文语境重译任意单句。
- 新增 TTS 发音词典；只替换送给语音模型的读法，不改变屏幕译文，并随课程配置保存。
- 新增“仅本机试听”，校对讲稿时可避免把草稿音频送进 Teams；支持导出双语 SRT、TXT 和 JSON 工程数据。
- 清除主窗口中已经废弃的旧长文播放线程、暂停事件和对应过时测试。

### 验证

- 97 项单元测试全部通过，包含长稿分批、发音词典、PCM 缓存、真实总时长、双语 SRT 导出和主窗口委托逻辑。
- 完成双语朗读台 Qt 离屏截图检查；本版本仍只保存在默认工作目录，不发布 GitHub。

## [1.4.0-local] - 2026-08-13

### 双语整段朗读台

- 长文本朗读升级为独立窗口：左侧中文、右侧英文，并按句对齐展示。
- 整段翻译会一次读取全文，复用课程领域、专有术语、翻译记忆和表达风格，避免逐句孤立翻译造成上下文不一致。
- 当前朗读句使用黄色高亮；根据真实 PCM 播放进度，用紫色同步标出中英文当前词或字。
- 每句显示预计时长，合成后替换为真实音频时长；底部新增总进度条、已用时间和总时间。
- 支持暂停、继续、停止、从头播放、拖动时间轴跳句，以及点击任意中英文句子从该句继续。
- 本版本仍为仅保存在本机默认工作目录的 local 版本，不同步桌面副本，也不发布到 GitHub。

## [1.3.2-local] - 2026-08-12

### 本机界面

- F8/F9 主按钮与播放、监听、停止等辅助按钮改为两个独立横排，修复窗口压缩时控件重叠。
- 会议时间轴默认可见最近 5 句话，更早记录仍可滚动查看。
- 每条时间轴记录均提供清晰的“▶ 重播”按钮，可单独重新合成并播放该句。
- 默认窗口调整为 1180×820，最小高度调整为 720。

### 验证

- 83 项单元测试全部通过，并完成 1180×820 Qt 离屏界面截图检查。

## [1.3.1-local] - 2026-08-12

### 修复

- FunASR 不再发送仅 Qwen-Audio Streaming 支持的内联 `vocabulary`；课程术语改用官方 `input.context`，并启用语言提示与心跳保活。
- FunASR WebSocket 意外断开后，在收到下一段音频时自动重连。
- Qwen3 HTTP TTS 同时返回流式音频和完整文件 URL 时不再重复播放同一句。
- 长文本朗读的暂停改为逐音频片段生效，不再等到整句播完。
- CosyVoice 3.5 Plus 的 Free-style 指令输入限制为 100 字符。

### 本机界面

- 首页重排为紧凑工作台，默认尺寸调整为 1180×760。
- 快捷输入与长文本朗读合并为页签，F8/F9、老师监听、停止和常用工具重新分组。
- 新增水晶蓝、深夜蓝黑、暖白琥珀、简洁浅色四套主题及顶部快速切换。
- 本机水晶雫插画只存放在被 Git 忽略的 `local_assets`，不会进入 GitHub；无本地素材时程序仍可正常运行。

### 验证

- 82 项单元测试全部通过，并完成 1180×760 Qt 离屏界面截图检查。

## [1.3.0] - 2026-08-10

### 新增

- 接入 Qwen3-TTS-VC 实时版（`qwen3-tts-vc-realtime-2026-01-15`）与高清版（`qwen3-tts-vc-2026-01-22`）声音复刻与合成，每个目标模型保存独立 voice。
- 接入阿里云 Fun-ASR 实时识别（`fun-asr-realtime`），支持术语表自动转为热词。
- 接入 CosyVoice V3 Flash / V3.5 Plus，V3.5 Plus 克隆相似度最高。
- 首页新增长文本朗读区：按句子切分、逐句播放、进度条、暂停/继续/停止。
- 历史时间轴每句新增重播按钮。
- 设置页引擎选择改为单选按钮，传统流水线时直译相关选项自动灰化。
- 设置页常用/高级分离：不常用配置折叠进「高级设置」面板。

### 修复

- 修复克隆成功后 voice/model 保存顺序错误，确保新 voice 写入目标模型而非旧模型。
- 统一 WebSocket 代理一致性：QwenRealtimeASR、FunASRRealtime、QwenLiveTranslate、Qwen3 Realtime TTS 均走设置页 HTTP 代理。
- Qwen3 HTTP 高清合成增加音频格式兜底：自动检测 WAV 头、支持 SSE 仅返回 URL 时下载解码。

### 验证

- 课程配置支持保存/加载 `tts_model` 与按模型区分的 `tts_voices`。
- 新增模型切换、克隆完成自动切换、课程配置音色隔离的 UI 回归测试。
- 新增 WebSocket 代理传递测试。
- 新增 Qwen3 HTTP WAV/URL 兜底测试。
- 共 75 项单元测试通过，并完成 Windows 离屏界面检查。

## [1.2.4] - 2026-08-03

### 修复

- 修复克隆接口返回 `voice_id` 后立即删除 OSS 样音、导致音色仍在部署时出现 TTS `431` 的问题；现在会等待百炼状态变为 `OK` 后再交付和清理样音。
- TTS 遇到 `431` 时会查询音色状态，并明确区分仍在处理、处理失败、模型不匹配和状态正常但引擎拒绝。
- 设置页滚动时，下拉框和数值框不再被鼠标滚轮误改。
- 自定义 F5/F8/F9 等快捷键后，会议首页按钮、提示文字和停止键会立即同步；保存后全局热键监听也会重启并生效。

### 界面

- “保存全部设置”移到设置页右上角，并增加并列的“编辑术语与提示词”入口。
- 使用传统流水线时仍可预先编辑直译模型和声音复刻配置，不再显示成无法点击。

### 验证

- 新增克隆音色部署、拒绝和模型不匹配测试；共 37 项单元测试通过，并完成 Windows 离屏界面检查。

## [1.2.3] - 2026-08-03

### 新增

- 会议控制台新增“F9 持续翻译”总开关：开启后按一次 F9 即持续监听，服务端 VAD 检测停顿并自动逐句翻译，再按一次停止。
- F9 持续模式运行时按住 F8，会自动终止持续翻译并切换到 F8 原声；松开 F8 后正常结束原声通道。

### 修复

- F9 按住模式不再为每句话新建 LiveTranslate WebSocket；同一程序会话会复用连接，使“服务端复刻一次”能在第二句及后续真正复用用户音色。
- 设置变化、取消或连接异常时会主动释放旧连接，下一次 F9 自动建立干净会话。

### 验证

- 新增持续模式 server VAD 参数、多轮状态重置和逐句结果回调测试；共 34 项单元测试通过。

## [1.2.2] - 2026-08-03

### 修复

- LiveTranslate 默认明确使用官方支持的“服务端复刻一次”，无需提前上传样音。
- 固定音色创建入口仅在选择高级固定音色模式后显示，并在调用前提示当前百炼兼容性风险。
- 遇到 `preprocess service not found` 时自动关闭创建窗口、切回“服务端复刻一次”并给出直接按 F9 使用的说明。
- 主窗口标题和页面标题显示当前版本号，便于确认是否误开旧版程序。

### 界面

- 声音复刻选项和帮助文字改为以可用方案为主，避免误把实验性固定音色接口当作首次配置步骤。

## [1.2.1] - 2026-08-03

### 修复

- 为 `qwen3.5-livetranslate-flash-realtime` 创建固定音色时，不再发送该模型不支持的服务端预处理参数，修复 `preprocess service not found`。
- LiveTranslate 目标模型会自动禁用不适用的“样音预处理”选项；本地 M4A/WAV 格式标准化和取样裁剪保持不变。
- 主窗口默认宽度由 1120 调整为 1280 像素，首次打开时可以直接看到更完整的双栏设置。

### 验证

- 新增 LiveTranslate 声音复刻最小请求回归测试，确保后续不会重新带入不支持的可选字段。

## [1.2.0] - 2026-08-03

### 新增

- 创建固定音色时可直接选择本地录音，不再要求用户手工准备公网 URL。
- 原生支持选择 iPhone `M4A`，并兼容 MP3、WAV、AAC、CAF、FLAC、OGG、OPUS、WMA 与 MP4。
- 内置 FFmpeg 自动把本地样音裁剪并转换为 24 kHz、单声道、16-bit PCM WAV，无需用户安装转换软件。
- 新增一次性 OSS 设置窗口；程序自动上传到私有 Bucket、生成 15 分钟签名 URL，并在复刻完成后立即删除临时样音。
- 百炼 API Key、OSS AccessKey ID 和 Secret 均使用 Windows 凭据管理器保存。

### 改进

- 小于 5 秒、空文件、超大文件或不支持的音频会在上传前给出清晰提示。
- OSS 删除失败时会显示临时对象完整路径，避免静默遗留含个人音色的文件。
- 固定 LiveTranslate 音色创建成功后继续自动填入 `voice_id`、切换固定音色并保存。

### 验证

- 新增 iPhone M4A 实际转码、WAV 参数、过短样音拒绝、OSS 上传/签名/删除和凭据隔离测试。
- 单元测试扩展到 30 项，并继续执行 Windows 离屏界面与 PyInstaller 打包验证。

## [1.1.0] - 2026-08-03

### 新增

- 新增 `Qwen3.5 LiveTranslate` 极速直译引擎：一次实时连接完成中文识别、英文翻译和英文语音流式播放。
- F9 默认启用极速直译，并显示松开按键至首段英文音频的实际耗时。
- 支持首次克隆、每次克隆、默认音色和固定直译 `voice_id` 四种音色策略。
- 设置页可一键创建仅供 LiveTranslate 使用的固定克隆音色。
- 保留原 ASR → Qwen-MT → Qwen-Audio-TTS 传统引擎，方便使用编辑确认、翻译记忆、领域和表达风格。

### 改进

- 极速模式自动锁定流式发送，避免“先确认”选项与已经播放的音频冲突。
- WebSocket 意外断开会立即报告原因，不再等待完整响应超时。
- API 连通性测试会根据当前引擎检查 LiveTranslate 或传统机器翻译模型。

### 验证

- 新增 LiveTranslate 会话参数、事件解析、音频流和异常断开单元测试。
- Windows 离屏界面启动测试覆盖默认极速模式与默认模型。

## [1.0.0] - 2026-08-02

首次公开发布。

### 新增

- 面向 Microsoft Teams 的中英双向语音翻译工作流。
- `F8` 原声通话与后台双语字幕，`F9` 中文转英文语音。
- Windows WASAPI 回环捕获老师/Teams 声音并生成中英字幕。
- 物理麦克风、系统声和混合会议三轨 WAV 录音。
- 可编辑确认、键盘文本发声、课程术语、翻译记忆与表达风格。
- 中英双语时间轴、歌词式悬浮字幕、搜索、崩溃恢复和课堂总结。
- SRT、TXT、WebVTT、Markdown 与 JSONL 字幕导出。
- Windows x64 便携发行包。

### 修复

- 保证连续启动的字幕缓存文件名唯一，避免 Windows 低精度系统时钟下偶发覆盖或无法恢复。

[1.2.0]: https://github.com/Viper-Boss/teams-voice-translator/releases/tag/v1.2.0
[1.2.4]: https://github.com/Viper-Boss/teams-voice-translator/releases/tag/v1.2.4
[1.2.3]: https://github.com/Viper-Boss/teams-voice-translator/releases/tag/v1.2.3
[1.1.0]: https://github.com/Viper-Boss/teams-voice-translator/releases/tag/v1.1.0
[1.0.0]: https://github.com/Viper-Boss/teams-voice-translator/releases/tag/v1.0.0
