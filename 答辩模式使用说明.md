# 答辩模式 · 使用说明

这是一个为论文答辩场景新做的精简实时翻译程序，与旧版 Teams 双向课堂翻译共存：
**旧程序原样保留（`run.bat`），答辩模式单独启动。**

## 启动方式

- **成品 exe（推荐）**：`dist\DefenseMode\DefenseMode.exe`，双击即可运行。
- 源码方式：双击 `run_defense.bat`（首次需先运行过一次 `setup.ps1` 安装依赖）。
- 修改源码后 exe 不会自动更新，需要重新打包：右键"使用 PowerShell 运行" `build_defense.ps1`，
  完成后新 exe 仍在 `dist\DefenseMode\DefenseMode.exe`。

## 它解决什么问题

- 默认按住 **F9** 说中文，松开后程序完成整句翻译，并用**你自己的克隆音色**说进 Teams。
  勾选「持续监听」后，F5/F9 改为单击开启、再次单击关闭；勾选本身不会自动打开翻译。
- 翻译不是一句一句孤立的：程序带着**整场答辩的对话记录 + 你的 PPT 摘要 + 强制术语表**
  翻译每一句，代词、话题、术语全场连贯一致。
- 按住 **F5** 临时说原声（有把握的英语可以直接说），声音原样直通 Teams。
- **Esc** 立即停止当前正在播放的英文。
- 评委的英文提问被自动转成屏幕上的中文字幕（需要选择回环设备），问题原文同时进入
  翻译上下文，你回答时翻译模型知道评委刚才问了什么。

## 快速开始

1. **启动**：双击 `dist\DefenseMode\DefenseMode.exe`。
2. **设置**：点右上角「设置」：
   - Workspace ID：与旧程序共用，填一次即可。
   - API Key：已保存在 Windows 凭据管理器（旧程序保存过就不用再填）。
   - **「测试连接」按钮**（API Key 下方）：填完就点它，会依次验证翻译大模型、实时识别、
     克隆音色三条链路（用的是当前输入框里的值，包括还没保存的 Key），音色有效时还会
     在本机播放一句试听。全绿再保存，不用等到答辩现场才发现问题。
   - **克隆音色 voice_id**：粘贴你在阿里云控制台复刻的音色 ID（必须与 TTS 模型绑定一致，
     默认模型 `qwen3-tts-vc-realtime-2026-01-15`），或点旁边「一键克隆音色…」现场复刻。
   - 设备四件套：麦克风选真实麦克风；程序输出选 `CABLE Input`；评委回环选 Teams 用的
     扬声器（戴上耳机）；试听设备选你自己听到声音的设备。四项默认锁定，先点「已锁定」
     解锁再选择，选好后重新锁定，避免滚动设置页时误改。
   - 语气样式：CosyVoice v3.5 等支持模型可从七种自然答辩语气中选择，也可以自定义。
     不支持指令的模型会自动禁用，避免把无效参数发给接口。
   - 背景环境：默认关闭；排练时可先试「极轻房间底噪」。它只在本机混入很轻的连续环境声
     并在句尾淡出，不会写进克隆样音、语音缓存或导出的会议语音。
   - 术语表：导入 PPT 后自动生成，也可以手工增删。
3. **导入 PPT**：点「导入 PPT」选答辩 PPT（.pptx）。程序会通读全文，自动生成：
   - 论文摘要（进入翻译上下文，让模型"懂"你的研究）；
   - 中英术语表（强制一致，比如"缺陷反演"永远译成论文里的固定说法）。
4. **自检**：点「自检」，逐项确认 Workspace/API Key/音色/虚拟声卡/麦克风/回环/连接/试听
   全部绿色，并听一下试听音色是不是你。
5. **Teams 设置**：Teams 里麦克风选 `CABLE Output`，扬声器选你的耳机。
6. **进会议**：点「开始答辩」（状态变待命）后只会打开评委声音识别；评委说话自动出中文字幕。
   你的麦克风保持关闭，轮到你回答时再按住 **F9** 说中文，或先勾选持续监听再单击 F9。

## 界面按钮与快捷键

- 主界面顶部有两个大按钮：**🎙 原声 (F5)** 和 **🌐 翻译 (F9)**——鼠标点击和按快捷键等效；
  激活时按钮**变绿**，一目了然。
- **🎙 原声**：未勾选持续监听时按住按钮或 F5 说话，松开停止；你的原声直接进会议。
- **🌐 翻译**：未勾选持续监听时按住按钮或 F9 说中文，松开停止收音并完成已收句子的翻译。
- **☑ 持续监听**：只改变两个按钮的操作方式，不会自动启动。勾选后单击原声或翻译开启，
  再点同一个按钮关闭；从一种模式切到另一种后，关闭时不会跳回前一种模式。
- **☑ 字幕悬浮窗**：屏幕底部出现歌词式双语字幕条——上行中文原句、下行正在朗读的英文，
  **说到哪个单词，黄色高亮就跳到哪个单词**。可用鼠标拖动位置，双击隐藏，Esc 立即消失；
  位置会自动记住。
- 快捷键都可以在 设置 → 快捷键 里改（原声/翻译/停止三个键都能换 F1~F12 或 Esc），
  保存后立即生效。

## 网络代理（开会开 VPN 的必看）

和外国导师开会时通常会开 VPN，但百炼是国内服务器——流量如果跟着 VPN 绕到国外再回来，
延迟会大很多。设置 → 网络代理 三档：

- **直连国内（默认，推荐）**：程序强制忽略系统代理和 VPN，全部流量直连阿里云北京；
  开着 VPN 也不受影响。
- **跟随系统代理**：需要流量走系统代理时选这个。
- **手动设置代理**：填 `127.0.0.1:7890` 这类地址，所有请求（含识别/合成的长连接）都走它。

改完代理建议点「测试连接」验证三条链路，并对比一下延迟数字。

## 音色库

「一键克隆音色」对话框顶部有**已克隆音色库**：程序里克隆过的音色自动入列；你在阿里云
控制台复刻的音色，点「把当前 voice_id 存入列表」也能收进来。双击或选中+「使用选中音色」
即可切换，绑定模型不一致时会提醒。从列表删除只是删记录，不会注销阿里云上的音色。

## 会议记录（录音 + 字幕自动归档）

勾选「自动录音」（默认开）后，每次点「开始答辩」会自动建一个会议文件夹：

```
<保存位置>/答辩_20260905_2130/
├── 录音/  我的麦克风_*.wav · 老师系统声_*.wav · 会议混音_*.wav（16kHz WAV）
├── 字幕/  中文字幕.txt · 英文字幕.txt · 双语字幕.txt
└── 会议全记录.md   ← 总文件：时间 | 谁 | 中文 | 英文 | 延迟 的完整表格
```

- 保存位置默认在 `我的文档/DefenseMode`，可在 设置 → 记录保存位置 修改。
- 结束答辩时自动落盘；会议中随时点「保存本场记录」，「打开记录文件夹」一键跳转。
- 没选评委回环设备时只录麦克风单轨，其余功能不受影响。
- 录音和字幕分开存放；「清空记忆」不会删除已保存的文件。

## 一键克隆音色（不用去阿里云控制台）

设置 → 克隆音色 voice_id 旁的「**一键克隆音色…**」。程序会根据你选的 **TTS 模型**自动切
换复刻通道（也可以在下拉里手动切换）：

- **Qwen3 直传通道**（模型为 `qwen3-tts-vc-*`）：安静房间朗读文字约 15 秒 → 停止录音 →
  创建，10~30 秒完成，无需任何额外配置。朗读文字可换成你的答辩开场白。
- **旧版 OSS 中转通道**（模型为 `cosyvoice-*` 或 `qwen-audio-tts-*`）：多一步需要你的
  私有 OSS（地域 + Bucket + RAM AccessKey），凭据只存 Windows 凭据管理器。流程：
  录音 → 自动转码并上传 OSS → 提交复刻 → **等待百炼部署 1~3 分钟**（窗口别关）→
  成功后自动删除 OSS 样音并填入 voice_id。
  - 建议给专用 RAM 用户只授 `oss:PutObject / GetObject / DeleteObject` 三项权限；
    Bucket 保持私有。

两个通道的 voice_id 互不通用：换 TTS 模型家族后需要用对应通道再克隆一次。

## 发声模型怎么选

| 模型 | 定位 |
|---|---|
| qwen3-tts-vc-realtime-2026-01-15 | 实时答辩默认；长连接低延迟 |
| qwen3-tts-vc-realtime-2025-11-27 | 实时旧快照备用 |
| qwen3-tts-vc-2026-01-22 | 高清更自然，延迟高；适合重播/预习 |
| **cosyvoice-v3.5-plus** | **克隆相似度全场最高**；觉得 Qwen3 音色不像时的对比首选 |
| cosyvoice-v3.5-flash / v3-plus / v3-flash / v2 / v1 | 逐代往前更旧更便宜 |

音色与模型严格绑定：每个 TTS 模型都要有自己复刻的 voice_id，切模型记得换 voice_id。

## AI 助答（主窗口第三栏）

- 监听评委发言，检测到提问时自动调用大模型，结合**论文 PPT 摘要 + 全场对话记忆 + 术语表**
  生成一段**第一人称、可直接口头回答**的答案，中文和英文对照显示。
- 大模型可以在面板右上角自己选（qwen-plus / qwen-flash / qwen-turbo / qwen-max，也可手填）；
  勾掉「自动生成」则只在点「↻ 重新生成」时才调用。
- 底部绿色大按钮「**▶ 一键回答**」：用你的克隆音色把这段英文回答直接说进会议，
  同时写入全场记忆，后续翻译保持连贯。**注意：这是辅助，不是替考——建议答辩前用它演练，
  现场上把它的回答当参考提纲，结合自己的理解说。**
- 评委的发言如果不是提问（点评、过渡语），面板会提示"未生成回答"，不会乱答。

## 讲稿预习（强烈建议答辩前一天做）

点「讲稿预习」：把中文开场陈述整段粘贴进去 → 点「① 整段连贯翻译」（结合 PPT 背景，
全文统一术语，比逐句翻译质量高一档）→ 点「② 从头朗读」，用你自己的音色反复听、跟读。
默认只在本机播放；勾选「同时输出到会议」才会送进 Teams。

「朗读语言」可选择**英文译文**或**中文原稿**。中文原稿无需先翻译，粘贴后即可直接从头
朗读；两种语言都支持暂停、继续、停止和黄色进度字幕。

## 延迟预期

界面顶部会显示每句"端到端延迟"（你说完 → 音色开始说英文）。正常约 1.5~2.5 秒：
识别断句 0.5 秒 + 大模型翻译 + 合成首音频。戴着耳机、网络正常时就是这个量级；
如果某句超过 4 秒，界面会标红提示。

## 应急手段

- **手动输入**：底部输入框输入中文回车 = 翻译并朗读；勾选「直接朗读英文」则原样朗读
  （适合某句译文你想自己改）。
- **时间轴**：每一句都有记录。某句翻译得不好，可以事后在时间轴检查；程序内正在播放时
  按 Esc 停止，再用输入框改说。
- **翻译失败自动跳过**：某一句合成失败不会卡住会议，界面会提示，你可以用输入框补一句。

## 与旧程序的关系

- 共用 `%APPDATA%\TeamsVoiceTranslator\settings.json` 里的设备选择、Workspace，
  共用 Windows 凭据管理器里的 API Key。
- 答辩模式自己的配置在 `%APPDATA%\TeamsVoiceTranslator\defense_settings.json`，
  不含任何密钥。
- 旧程序功能不受影响；答辩当天万一新模式出问题，随时可以切回旧版 `run.bat`。

## 费用参考

一场 30 分钟答辩（你总共说约 10 分钟）：实时识别 + 翻译大模型 + 语音合成全部加起来
通常不到 1 元人民币。

## 技术细节（给排障用）

- 识别：`qwen3-asr-flash-realtime`（服务端 VAD 自动断句，静音阈值可在设置调 200~1500ms）。
- 翻译：`qwen-plus` 流式输出，系统提示词 = 论文摘要 + 强制术语表 + 最近 N 轮对话
  （句数可调），英文按句切分边译边合成。
- 合成：`qwen3-tts-vc-realtime` WebSocket **长连接复用**，整场会议一个连接，句间零握手；
  断线自动重连、失败自动重试一次。
- 播放：PCM 24 kHz → `CABLE Input`（+可选本机试听）；自己播放英文期间会短暂屏蔽回环
  采集，防止翻译腔被误识别成评委提问。
